info:
  name: config
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:7000/sys/firmware/config
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
